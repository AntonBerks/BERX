/** Node entry for the stability gate: the decisions, without a GPU. */
export {
	BERX_LOD_HYSTERESIS,
	BERX_BUDGET_HYSTERESIS,
	BERX_TIER_SETTLE_FRAMES,
	berxStableLod,
	berxBudgetDistance,
	berxTierSettled,
	berxSettleTier,
	berxRememberFrame,
	BERX_NO_MEMORY,
} from '@berx/spatial';
export {BERX_LOD_DISTANCE} from '@berx/spatial';
