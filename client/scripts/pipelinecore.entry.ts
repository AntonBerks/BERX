/** Node entry for the pipeline gate: the declaration, nothing else. */
export {
	BERX_PIPELINE,
	berxPipelinePasses,
	berxPipelineViolations,
	berxExpectedPasses,
	berxPipelineStage,
} from '@berx/spatial';
